<UserControl x:Class="Bioingenieria.Controls.EquipmentCardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,8">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource PrimaryBrush}" CornerRadius="9,0,0,9" />

            <StackPanel Grid.Column="1" Margin="14,10,10,10">
                <TextBlock x:Name="TitleTextBlock"
                           FontWeight="Bold"
                           FontSize="14"
                           Foreground="{StaticResource PrimaryDarkBrush}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,6" />

                <WrapPanel x:Name="ChipsPanel" />

                <StackPanel x:Name="ActionsPanel"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,8,0,0">
                    <Button x:Name="DeleteButton"
                            Content="Eliminar"
                            Style="{StaticResource DangerButtonStyle}"
                            Click="DeleteButton_Click" />
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
