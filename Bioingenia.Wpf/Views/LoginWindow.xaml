<Window x:Class="Bioingenieria.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Iniciar sesión"
        Height="370" Width="320"
        Background="{StaticResource SurfaceBrush}"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Icon="/Resources/app.ico">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Image Grid.Row="0" Source="/Resources/logo.png" Width="84" Height="84" Margin="0,20,0,0" HorizontalAlignment="Center" />

        <TextBlock Grid.Row="1"
                   Text="Bioingenia"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="{StaticResource PrimaryDarkBrush}"
                   HorizontalAlignment="Center"
                   Margin="0,10,0,20" />

        <StackPanel Grid.Row="2" Margin="30,0,30,20">
            <TextBlock Text="Usuario" Foreground="{StaticResource TextBrush}" Margin="0,0,0,4" />
            <TextBox x:Name="UsernameTextBox" Height="27" Margin="0,0,0,16" />

            <TextBlock Text="Contraseña" Foreground="{StaticResource TextBrush}" Margin="0,0,0,4" />
            <PasswordBox x:Name="PasswordBox" Height="27" Margin="0,0,0,8" />

            <TextBlock x:Name="ErrorTextBlock"
                       Foreground="{StaticResource CriticalBrush}"
                       TextWrapping="Wrap"
                       Visibility="Collapsed"
                       Margin="0,0,0,16" />

            <Button Content="Iniciar sesión"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Height="36"
                    IsDefault="True"
                    Click="LoginButton_Click" />
        </StackPanel>
    </Grid>
</Window>
